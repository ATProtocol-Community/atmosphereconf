---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class:list={[
    "bg-card text-card-foreground flex flex-col rounded-xl border py-8 px-12 shadow-sm",
    className,
  ]}
>
  <slot />
</div>
