{
  "name": "@atmosphereconf/2027",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "start": "node ./dist/server/entry.mjs",
    "db:init": "node --env-file-if-exists=.env scripts/init-db.mjs",
    "postinstall": "lex build --clear --indexFile",
    "update-lexicons": "lex install --update && lex build --clear --indexFile"
  },
  "dependencies": {
    "@astrojs/node": "^11.1.5",
    "@atproto/lex": "^0.3.11",
    "@fujocoded/authproto": "^0.4.0",
    "@libsql/client": "^0.18.0",
    "astro": "^7.3.1",
    "interestfor": "^1.0.8",
    "node-addon-api": "^8.9.2",
    "node-gyp": "^13.0.2"
  }
}
